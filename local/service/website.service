[Unit]
Description=Load chrome app
After=network.target

[Service]
Type=oneshot
ExecStart=nohup /usr/bin/chromium-browser --enable-features=WebContentsForceDark --kiosk --app="https://reorg.goobill.com" &

[Install]
WantedBy=default.target
